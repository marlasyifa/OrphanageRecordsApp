<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="marla.com.pantiku">

    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/iconku"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".Konfirmasi1" />
        <activity android:name=".Konfirmasi2" />
        <activity android:name=".HalamanPanti1" />
        <activity android:name=".Detail" />
        <activity android:name=".MainGambar" />
        <activity android:name=".ProsesKirim" />
    </application>

</manifest>